const { Router } = require('express');
const { Cemetery } = require('../models/Cemetery');

module.exports = Router()
  .get('/', async (req, res, next) => {
    try {
      const data = await Cemetery.getAll();
      res.json(data);
    } catch (e) {
      next(e);
    }
  })
  .get('/:id', async (req, res, next) => {
    try {
      const data = await Cemetery.getById(req.params.id);
      if (!data) {
        next();
      }
      res.json(data);
    } catch (e) {
      next(e);
    }
  })
  .post('/', async (req, res, next) => {
    try {
      const data = await Cemetery.insert(req.body);
      res.json(data);
    } catch (e) {
      next(e);
    }
  })
  .put('/:id', async (req, res, next) => {
    try {
      const data = await Cemetery.updateById(req.params.id, req.body);
      res.json(data);
    } catch (e) {
      next(e);
    }
  })
  .delete('/:id', async (req, res, next) => {
    try {
      const data = await Cemetery.delete(req.params.id);
      res.json(data);
    } catch (e) {
      next(e);
    }
  });
